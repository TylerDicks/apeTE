<%= form_for(@plan) do |f| %>
  <% if @plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@plan.errors.count, "error") %> prohibited this plan from being saved:</h2>

      <ul>
      <% @plan.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<table>
  <thead>
    <tr>
      <th>Terms</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
     <% @plan.terms.each do |term|  %>
      <tr>
        <td><%= term.semester %> <%= term.year.to_s %></td>
        
      </tr>
    <% end %>  
  </tbody>
</table>

